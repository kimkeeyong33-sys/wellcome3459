@AGENTS.md

# 작업 규칙

1. 새 세션을 시작하면 항상 `PROGRESS.md`를 먼저 읽고 현재 상태(진행 상황, 다음에 할 일)를 파악한 뒤 작업을 시작한다.
2. 작업 단위(기능 하나, 버그 수정 하나)가 끝날 때마다 자동으로 커밋하고 푸시한다. 이 파일에 의해 사전 승인된 작업이므로 매번 커밋/푸시 여부를 다시 확인할 필요는 없다 — 단, 아래 4번 규칙(기존 기능 관련 브랜치/PR)은 예외 없이 지킨다.
3. 큰 변경사항(새 기능 추가, 구조 변경 등)이 생기면 `PROGRESS.md`를 최신 상태로 업데이트한다.
4. 카카오 로그인, 경매/입찰 등 기존 핵심 기능을 건드리는 작업은 반드시 별도 브랜치를 만들어 PR로 진행하고, main 브랜치(기본 브랜치)에 직접 커밋하지 않는다.
